name: Security CI Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  security-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # --- SAST (Static Application Security Testing) ---
      # Используем bandit, как указано в задании для Python
      - name: SAST Scan (Bandit)
        run: |
          echo "Запуск статического анализа кода (SAST) с помощью Bandit..."
          bandit -r . -f json -o bandit-report.json || true
          bandit -r . -f screen

      # --- SCA (Software Composition Analysis) ---
      # Используем OWASP Dependency-Check, как указано в задании
      - name: SCA Scan (OWASP Dependency-Check)
        uses: dependency-check/Dependency-Check_Action@main
        id: depcheck
        with:
          project: 'secure-api-lab'
          path: '.'
          format: 'HTML'
          args: >
            --scan .
            --exclude '**/venv/**'
            --failOnCVSS 7

      # Сохраняем отчеты как артефакты, чтобы их можно было скачать
      - name: Upload Security Reports
        uses: actions/upload-artifact@v3
        with:
          name: security-reports
          path: |
            bandit-report.json
            ${{ github.workspace }}/reports
